import { RegisterpipePipe } from './registerpipe.pipe';

describe('RegisterpipePipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterpipePipe();
    expect(pipe).toBeTruthy();
  });
});
